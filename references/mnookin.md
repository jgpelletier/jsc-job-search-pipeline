---
# Structured fields for programmatic access (db.get_must_haves, etc.).
# Edit the frontmatter AND the body below in lockstep — they should agree.
must_haves:
  -
  -
  -
must_nots:
  -
  -
  -
soft_preferences:
  -
  -
---

# Hard Constraints — Must-Haves and Must-Nots

> The `score-fit` and `analyze-jd` skills read this file to filter roles. A missing must-have or a present must-not should drive a recommend-close, not a low score.
>
> Named "mnookin" after the Robert Mnookin negotiation framework (decide your walk-away criteria before the conversation, not during).
>
> The frontmatter at the top of this file mirrors the lists below for programmatic access. Edit both. The narrative body is what the LLM reads at session start; the frontmatter is what `db.get_must_haves()` / `db.get_must_nots()` return.

## Must-haves
A role must satisfy every item on this list to be worth pursuing.

-
-
-

## Must-nots
A single match here is enough to disqualify a role.

-
-
-

## Soft preferences
Nice-to-haves that influence ranking but do not disqualify.

-
-
